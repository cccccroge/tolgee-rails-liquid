<!DOCTYPE html>
<html>
  <head>
    <title>TolgeeRailsLiquid</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <% if params[:mode] == 'development' %>
      <%= javascript_importmap_tags %>
    <% end %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
